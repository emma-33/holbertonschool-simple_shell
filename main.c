#include <stdio.h>
/**
 * function main
 * 
 * Return: Always 0
*/
{
    char *lineptr;
    constant_char *delim;
    size_t n;

    printf("$");

    _getline (lineptr, n);
    {
        if (_getline == -1)
        return (-1);
    }
    strtok (lineptr, delim, getline);
    {
        if (_strtok == -1)
        return (-1);

    }
    wait();
    {
        if (_wait == _1)
        return (-1);
    }

    free (str_copy);
    free (array);
    return(0);

}